Асинхронное приложение на Python для загрузки данных из API и сохранения их в базу данных с использованием SQLAlchemy ORM.

## 🛠 Инструкция по запуску

### 1. Подготовка окружения
Убедитесь, что у вас установлен Python 3.9+. Создайте и активируйте виртуальное окружение:


# Создание окружения
python -m venv venv

# Активация для Linux/macOS:
source venv/bin/activate

# Активация для Windows:
venv\Scripts\activate
Используйте код с осторожностью.

### 2. Установка зависимостей
Установите необходимые пакеты (используется aiosqlite для работы с SQLite в асинхронном режиме):
bash
pip install aiohttp sqlalchemy aiosqlite
Используйте код с осторожностью.

### 3. Настройка базы данных
По умолчанию проект использует SQLite (файл db.sqlite3 создастся автоматически).
Если вы хотите использовать PostgreSQL, установите переменную окружения перед запуском:
bash
# Пример для Linux/macOS
export SQLALCHEMY_PG_CONN_URI="postgresql+asyncpg://user:password@localhost/dbname"

# Пример для Windows (PowerShell)
$env:SQLALCHEMY_PG_CONN_URI="postgresql+asyncpg://user:password@localhost/dbname"
Используйте код с осторожностью.

### 4. Запуск проекта
Запустите главный файл приложения:
bash
python main.py
